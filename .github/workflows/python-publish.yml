name: Print Tags

on:
  push:
    branches:  main   # You can adjust the branches to monitor

jobs:
  print-tags:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Print Tags
        run: |
          git tag  # Print all tags in the repository
